import SimpleTestDashboard from './SimpleTest'

function Dashboard({ gateway }) {
  // For now, show the simple test version to debug deployment issues
  return <SimpleTestDashboard />

}

export default Dashboard